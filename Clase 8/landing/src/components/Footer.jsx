import React from "react";

const Footer = ( props ) => {
    // Codigo JS de este componente
    // const anio = new Date().getFullYear();
    return (
        <div>
            <h3>El año es {props.year}</h3>
        </div>
    );
}

export default Footer;